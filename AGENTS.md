# 项目协作约定

## 发布方式

- 本项目通过 GitHub 管理和发布，不使用 OpenAI Sites。
- 即使仓库中存在 `site/.openai/hosting.json`，也将其视为遗留配置：不要调用 Sites 建站或托管流程，不要创建或部署 Sites，也不要为发布方式反复向用户确认。
- 用户已明确授权：本项目后续每次完成修改和验证后，直接提交并推送到 GitHub 的 `main`，无需再次确认。
- 沿用当前 Git 仓库与远程地址，不改用其他托管平台。
